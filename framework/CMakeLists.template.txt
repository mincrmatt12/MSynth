cmake_minimum_required(VERSION 3.3)
project(bucket_name_here)

include(${CMAKE_CURRENT_LIST_DIR}/../framework/build.cmake)  # point at the right dir

# setup an APP
add_app(
	# folder /target name
	factory_app
	# name
	"FactoryTest"
	# appID
	"FTsc"
	# flags
	TEST
	LOWSCREEN
	FS
	# valid options are: PRIMARY TEST EEPROM FS LOWSCREEN APPCMD
)

# To mess with app building, change settings on app_${target_name}

add_bucket(
	# target name
	factory
	# how large of a bucket this is, valid options are 16, 32, 64, or multiple of 128
	16
	# apps
	factory_app
)
